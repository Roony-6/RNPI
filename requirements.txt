# Backend Framework & ORM
fastapi==0.136.3
uvicorn==0.49.0
SQLAlchemy==2.0.50
pydantic==2.13.4
email-validator==2.3.0
python-multipart==0.0.32

# Database
psycopg2==2.9.12
alembic==1.13.0

# Authentication & Security
bcrypt==5.0.0
python-jose==3.5.0

# Environment & Configuration
python-dotenv==1.2.2

# Runtime dependencies (auto-installed by above)
# - cryptography, cffi, ecdsa, pyasn1, rsa (from python-jose)
# - greenlet, anyio, h11, starlette (from fastapi/uvicorn)
# - pydantic-core, typing-extensions (from pydantic)
